/**
 * הגבלת קצב למסלול ההזמנה הציבורי.
 *
 * ═══ למה זה קיים ═══
 *
 * משלב 15B, `/booking` אינו דורש OTP. שתי ההגנות שנשארות מפני תפיסת
 * סלוטים המונית הן:
 *
 *   1. `max_active_pending_per_customer` (=2) — מגביל לקוחה בודדת.
 *   2. המגבלה כאן — מגבילה מקור בודד שמנסה מספרי טלפון רבים.
 *
 * ═══ 🔒 האכיפה אינה כאן ═══
 *
 * הקובץ הזה מחזיק את **ההגדרה** בלבד. האכיפה בפועל היא ב-SQL
 * (`create_public_pending_appointment`, 0018), בתוך אותה טרנזקציה שיוצרת
 * את התור ומאחורי `pg_advisory_xact_lock` על ה-IP.
 *
 * הסיבה זהה לזו של 0013: "לספור ואז ליצור" כשתי קריאות נפרדות מאפשר
 * לחמש בקשות מקבילות לעבור את הספירה יחד. בדיקה מוקדמת בקוד היא שיפור
 * חוויה — לא ההגנה.
 *
 * ⚠️ הערך כאן חייב להישאר תואם לתקרה הקשיחה ב-0018. אותו חוזה בדיוק
 * שקיים בין lib/otp.ts ל-issue_otp_atomic, ובדיוק מאותה סיבה: המספר
 * ב-SQL הוא מה שמחזיק גם מול קורא שגוי. scripts/test-booking-ratelimit.mjs
 * קורא את שני הקבצים ונכשל אם הם נפרדו.
 */

/** מקסימום יצירות הזמנה ציבוריות **מוצלחות** מאותו IP בשעה */
export const PUBLIC_BOOKING_MAX_PER_IP_PER_HOUR = 5

/** חלון הספירה, בשעות. תואם ל-`interval '1 hour'` ב-0018. */
export const PUBLIC_BOOKING_RATE_WINDOW_HOURS = 1

/**
 * ⚠️ **נספרות יצירות שהצליחו בלבד.**
 *
 * בקשה שנפלה על השעה התפוסה מגלגלת את הטרנזקציה לאחור, כולל רישום אירוע
 * הקצב. לקוחה אמיתית שנתקלה שלוש פעמים ב"השעה נתפסה" אינה שורפת את
 * המכסה שלה — הנזק שהמגבלה מונעת הוא תפיסת סלוטים, ובקשה שנכשלה לא
 * תפסה דבר.
 */
export const PUBLIC_BOOKING_RATE_COUNTS_ONLY_SUCCESS = true

/**
 * ⚠️ הנוסח מזכיר במפורש את WhatsApp. מפעילות הסלולר בישראל עובדות
 * ב-CGNAT, ולכן לקוחות רבות חולקות כתובת ציבורית אחת — חסימה כאן יכולה
 * לפגוע בלקוחה אמיתית לגמרי. היא חייבת לקבל מסלול חלופי, לא מבוי סתום.
 */
export function bookingRateLimitMessage(): string {
  return 'התקבלו יותר מדי בקשות תור מהחיבור הזה בשעה האחרונה. אפשר לשלוח לנו הודעה בוואטסאפ ונשמח לעזור.'
}
