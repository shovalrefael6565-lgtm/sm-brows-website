# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem
.env*
!.env.example
*.tsbuildinfo

# debug
npm-debug.log*

# IDE
.vscode/
.idea/

# claude internals
.claude/*
!.claude/skills/
.claude/skills/*
# vendored SEO skills are tracked (see each skill's PROVENANCE.md)
!.claude/skills/seo-mastery/
!.claude/skills/ai-visibility-optimizer/

# service account keys
sm-brows-*.json
*-service-account*.json
video-originals/

# מקורות וידאו גולמיים (MOV מהטלפון) שיושבים ב-public/ — לעולם לא נכנסים
# לריפו ולא נפרסים: מה שמוגש בפועל הוא הגרסאות הדחוסות public/videos/*.mp4.
# בלי זה, git add -A היה מכניס 36MB לריפו ומעלה אותם לאתר.
public/**/*.MOV
public/**/*.mov
