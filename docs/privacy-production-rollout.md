# Runbook — פריסת שלבים 9A–9C לפרודקשן

**סוג מסמך:** תוכנית תפעולית (9D.2 — הכנה בלבד). אינו ייעוץ משפטי.
**תאריך כתיבה:** 2026-08-17
**Checkpoint:** `cbdee8f` (9C)

**אין במסמך זה נתוני לקוחות אמיתיים ואין ערכי env** — שמות משתנים בלבד.

---

## 0. סטטוס — קרא לפני הכול

🔴 **שום שלב במסמך הזה טרם בוצע.** נכון לכתיבת שורות אלה:

- **מיגרציות `0031` ו-`0032` טרם הופעלו** על שום מסד נתונים — לא dev ולא prod.
- **ארבעת ה-commits טרם נדחפו** ל-remote וטרם נפרסו.
- **retention scheduler כבוי** — אין `vercel.json`, אין הגדרת cron, אין חיבור ל-QStash.
- **שני כלי ה-Calendar מעולם לא הורצו** — לא dry-run ולא execute.

אין לעדכן מסמך זה כך שישתמע שהפריסה או המיגרציות בוצעו, עד שהן אכן יבוצעו.

---

## 1. למה נדרש חלון תחזוקה — הסיכון של 0031

`0031` **מוחקת** את חתימות ה-RPC הישנות ויוצרת חדשות עם שני פרמטרים נוספים
(`p_privacy_notice_version`, `p_privacy_notice_acknowledged`), **ללא `DEFAULT`**.
PostgREST מזהה פונקציה לפי שמות הארגומנטים בגוף הבקשה — ולכן:

| מצב | תוצאה |
|---|---|
| app ישן + DB ישן | ✅ עובד (בלי acknowledgement) |
| **app ישן + DB אחרי 0031** | 🔴 **שני מסלולי ההזמנה נכשלים** |
| **app חדש + DB ישן** | 🔴 **שני מסלולי ההזמנה נכשלים** |
| app חדש + DB אחרי 0031 | ✅ עובד, acknowledgement נשמר |

**אין סדר פריסה בטוח.** לכן: סוגרים את שער ההזמנות *לפני* שנכנסים למצב מעורב.

`0032` לעומת זאת **בטוחה בכל ארבעת המצבים** (אפס `DROP`, הכל `create or replace`
+ `add column if not exists`, ו-`get_crm_customer` שומרת חתימה וטיפוס החזרה).

**המנגנון:** `NEW_BOOKING_SYSTEM_ENABLED=false` חוסם את שני מסלולי ההזמנה
ב-403 **לפני** כל קריאת RPC, עם הודעה כנה והפניה לוואטסאפ
(`app/api/bookings/request/route.ts`, `app/api/appointments/route.ts`).
כך אף בקשה לא נכשלת ב-500, ואף תור לא נכתב ללא acknowledgement.

---

## 2. תנאי התחלה — כולם חובה

- [ ] **חלון פעילות נמוכה.** לא בשבת (המערכת ממילא חסומה אז), ולא בשעות שיא.
- [ ] **גיבוי Supabase עדכני, ויכולת ה-restore אומתה בפועל** — לא רק שהגיבוי קיים.
- [ ] `npm run typecheck`, `npm run lint`, `npm run test` (ללא live), `npm run build` — כולם ירוקים.
- [ ] **אישור מפורש של בעלת העסק לפני כל פעולה חיה.** כל שלב בסעיף 3 מתחיל רק אחרי אישור.
- [ ] הוכן מראש (ולא הורץ) SQL rollback שמשחזר את החתימות הישנות —
      `create_public_booking_request` (12 פרמטרים) ו-`create_personal_area_booking_request` (9).

---

## 3. סדר ה-rollout — מחייב, ללא דילוגים

> ⚠️ **אין להניח ששינוי משתנה סביבה ב-Vercel משפיע בלי redeploy.** כל שינוי
> של `NEW_BOOKING_SYSTEM_ENABLED` מחייב redeploy/promote ואימות בפועל שהערך
> החדש אכן תפס — לא להסתמך על עריכת המשתנה לבדה.

1. **הגדר ב-Production: `NEW_BOOKING_SYSTEM_ENABLED=false`.**
2. **פרוס את HEAD** (`cbdee8f` או צאצא מאומת שלו).
3. **אמת שהגרסה החיה החדשה אכן עלתה** — לפי מזהה ה-deployment ב-Vercel, לא לפי הנחה.
4. **אמת שהשער סגור, לפני שנוגעים ב-DB:** בצע POST בטוח **ללא נתוני לקוחה**
   לשני המסלולים (`/api/bookings/request`, `/api/appointments`) וּודא ששניהם
   מחזירים **403** — כלומר נחסמו **לפני** קריאת ה-RPC.
   🔴 אם אחד מהם אינו מחזיר 403 — **עצור. אל תריץ מיגרציות.**
5. **רק אחרי ששלב 4 אומת:** הרץ `0031`, ואז `0032` — **בסדר הזה**.
6. **אמת דרך קטלוג ה-DB / שאילתות read-only בלבד** שהחתימות והעמודות קיימות:
   שתי הפונקציות בחתימתן החדשה (14 ו-11 פרמטרים), העמודות
   `appointments.privacy_notice_version` / `privacy_notice_acknowledged_at`,
   והעמודות `customers.retention_hold*`. **קריאה בלבד — אין כתיבה בשלב האימות.**
7. **הגדר `NEW_BOOKING_SYSTEM_ENABLED=true`.**
8. **בצע redeploy נוסף** כדי שהערך יתפוס, ואמת שהוא אכן תפס.
9. **הרץ את ה-smoke checklist המלא** (סעיף 5).

---

## 4. תרחישי כשל

| תרחיש | פעולה |
|---|---|
| **ה-deployment הראשון (שלב 2) נכשל** | 🟢 **אל תריץ מיגרציות.** ה-DB עדיין ישן והאפליקציה הישנה עדיין חיה — מצב תקין לחלוטין. החזר `NEW_BOOKING_SYSTEM_ENABLED=true` (עם redeploy) והאתר חוזר לתפקוד מלא. תקן ונסה שוב במועד אחר. |
| **המיגרציות הצליחו, ה-deployment השני (שלב 8) נכשל** | 🔴 **השאר את ההזמנות סגורות (`false`) ותקן קדימה.** **אין להחזיר את האפליקציה הישנה מול DB חדש** — זה בדיוק המצב השבור, וכל בקשת תור תיכשל ב-500. rollback של Vercel **אינו** מבטל את המיגרציה. |
| **צריך להחזיר את ה-DB אחורה** | 🔴 **מוצא אחרון בלבד, ודורש אישור נפרד ומפורש של בעלת העסק.** להשתמש ב-SQL ה-rollback שהוכן בסעיף 2, ורק אם fix-forward אינו אפשרי. |
| **booking נכשל אחרי שהכול "הצליח"** | כבה מיד (`false` + redeploy), ובדוק בלוגי Supabase את קוד השגיאה. `PGRST202` = אי-התאמת חתימה, כלומר מיגרציה שלא רצה או רצתה חלקית. |

**🔒 בכל תרחיש: אין להציג ללקוחה הצלחה אם הבקשה לא נשמרה.** הקוד הקיים כבר
עומד בזה — כשל מחזיר 500 מפורש ("הבקשה לא נשמרה") עם הפניה לוואטסאפ, ושער
סגור מחזיר 403 מפורש. אין לשנות זאת ל"נראה בסדר" בשום מצב.

---

## 5. Smoke checklist (אחרי שלב 8)

> 🔒 **נתוני בדיקה סינתטיים בלבד** — מספר בדיקה ייעודי ותאריך רחוק בעתיד
> (הדפוס הקיים: `2028-06-06`, ר' `scripts/test-appointment-approval-live.mjs`).
> **ניקוי נתוני הבדיקה בסיום דורש אישור מפורש של בעלת העסק בזמן הפעולה** —
> מחיקה בפרודקשן אינה פעולה שגרתית.

- [ ] **public booking עם acknowledgement** → 201, והתור נשמר
- [ ] **public booking בלי acknowledgement** → **400** `privacy_not_acknowledged`
- [ ] **personal-area booking** → מצליח
- [ ] **עמוד לקוחה בניהול** נטען, ומתג ה-retention מוצג
- [ ] **consent:** לפני הסכמה — אין בקשות רשת ל-GA/Meta; אחרי — יש
- [ ] **תמונות:** hero והגלריה נטענות (כולל `/hero.webp?v=2`)
- [ ] **headers:** CSP קיים; `private, no-store` על `/api/admin/*`,
      `/api/appointments/*`, `/api/auth/*` ו-`/api/internal/*`;
      `/api/bookings/slots` נשאר ציבורי
- [ ] **`POST /api/internal/privacy-retention` → 404** (הסוד לא מוגדר)

---

## 6. אחרי הפריסה — מה נשאר כבוי

- **`PRIVACY_RETENTION_SECRET` נשאר לא מוגדר.** ה-route מחזיר 404 וזו ברירת
  המחדל הרצויה בפריסה הראשונה.
- **אין scheduler.** לא Vercel Cron ולא QStash.
- **Calendar dry-run מותר רק אחרי** שאומת בפועל שאירוע חדש שנוצר בפרודקשן
  **אינו מכיל טלפון ב-description** — לא לפי commit, אלא לפי בדיקה בפועל.
  ר' `docs/calendar-phone-cleanup-runbook.md`.
- **Format A נשאר audit read-only**, והניקוי שלו ידני ע"י בעלת העסק.
- **🔒 אין `execute` — לא של retention ולא של Calendar — בלי אישור נפרד ומפורש.**
